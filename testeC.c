#include <stdio.h>

int main() //programa para determinar comprimento e largura de retangulo
{
    int largura, comprimento, area;

    printf("Digite a Largura do retangulo: \n");
    scanf("%d", &largura); 

    printf ("Digite o comprimento do retangulo: \n");
    scanf("%d", &comprimento);w

    area = largura * comprimento; 

    printf("A area do retangulo e: %d\n" area,);
    return 0;
}